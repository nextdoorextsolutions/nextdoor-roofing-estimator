# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES (Vercel)
# =============================================================================
# Set these in Vercel Dashboard → Settings → Environment Variables

# Google Maps API Key (REQUIRED)
# Get your API key from: https://console.cloud.google.com/apis/credentials
# Required APIs: Maps JavaScript API, Places API, Geocoding API
VITE_GOOGLE_MAPS_KEY=your_google_maps_api_key

# Backend API URL (REQUIRED after deploying to Render)
# Example: https://nextdoor-backend.onrender.com/api/trpc
VITE_API_URL=https://your-backend.onrender.com/api/trpc

# OAuth Portal Configuration (Optional - for admin login)
VITE_OAUTH_PORTAL_URL=
VITE_APP_ID=

# Analytics (Optional)
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=

# =============================================================================
# BACKEND ENVIRONMENT VARIABLES (Render)
# =============================================================================
# Set these in Render Dashboard → Environment Variables

# Node Environment
NODE_ENV=production

# Database Connection (REQUIRED)
# Get from Render PostgreSQL or Supabase
DATABASE_URL=postgresql://user:password@host:5432/database

# Supabase Configuration (Optional - for authentication)
SUPABASE_URL=
SUPABASE_JWT_SECRET=
SUPABASE_ANON_KEY=

# Google Maps API Key (same as frontend)
VITE_GOOGLE_MAPS_KEY=your_google_maps_api_key
